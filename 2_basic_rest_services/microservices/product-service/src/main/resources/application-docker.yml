spring:
  config:
    activate:
      on-profile: docker
  data:
    mongodb:
      host: mongodb
  cloud:
    stream:
      kafka:
        binder:
          brokers: kafka
  rabbitmq:
    host: rabbitmq

server:
  port: 8080
